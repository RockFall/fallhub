/.gradle
/captures/
/local.properties
# Keep gradle wrapper + scripts in git so clones can build Android without
# regenerating the Flutter android/ scaffold.
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your Play Store keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Sideload CI key (ADR-035) — versioned on purpose so updates stay in-place.
!keystore/sideload.keystore
